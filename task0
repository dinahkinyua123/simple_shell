#include "Shell.h"
#include <stdio.h>
/**
 * multiply_numbers - Multiplies two integers.
 * @x: First integer.
 * @y: Second integer.
 *
 * Return: The product of two integers.
 */
int multiply_numbers(int x, int y)
{
    int product;
    product = x * y;
    return product;
}

/**
 * main - Entry point of the program.
 *
 * Return: (0) perfect.
 */
int main(void)
{
    int result = multiply_numbers(9, 7);
    /* Use the result variable as needed */
    return 0;
}

