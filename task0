#include "Shell.h"
#include <stdio.h>
/**
 * multiply_numbers - Multiplies two integers.
 * @x: First integer.
 * @y: Second integer.
 *
 * Return: The product of two integers.
 */
int multiply_numbers(int x, int y)
{
    int product;
    product = x * y;
    return product;
}

/**
 * main - Entry point of the program.
 *
 * Return: 0 on success.
 */
int main(void)
{
    int result = multiply_numbers(10, 8);
    /* Use the result variable as needed (e.g., pass it to other functions or use it further) */
    return 0;
}

